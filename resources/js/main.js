const array = ['PRIMERO', 'SEGUNDO', 'TERCERO','CUARTO'];

function mostrarEnConsola(elemento, index, array) {
    console.log(elemento);
}

array.forEach(insertCard)

function insertCard(movie, index, movies) {
    
}